const AuthLayout = ({children}) => {
    return (
        <div className="flex items-center justify-center w-full h-full bg-white/95">
            {children}
        </div>
    )
}

export default AuthLayout